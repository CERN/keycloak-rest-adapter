Flask
gunicorn==19.4.*
requests==2.22.*
# We had to fork the repo in order to allow the redirect url to work
flask-restplus-oauth-redirect
# From urllib3 docs: urllib3 (requests) will try to load the default system certificate stores
# The most reliable cross-platform method is to use the certifi package which provides Mozilla's root certificate bundle
certifi
authlib==0.12.*